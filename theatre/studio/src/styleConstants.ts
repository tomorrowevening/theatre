/**
 * Shared style constants for Theatre Studio UI components.
 * Use these instead of hardcoded pixel values to keep the UI consistent.
 */

/** Height of the visual animation bar in the dope sheet (e.g. SubSequenceBar, AggregateKeyframeBar). */
export const BAR_HEIGHT = 24

/** Standard height for interactive controls: buttons, text inputs, popover rows. */
export const CONTROL_HEIGHT = 20

/** Width of a bar's resize handle grip. */
export const HANDLE_WIDTH = 7

/** Width of the expanded hover/click area for bar resize handles. */
export const HANDLE_HOVER_WIDTH = 15

/** Width of the hex colour text input used in colour pickers. */
export const HEX_INPUT_WIDTH = 70
